"use client";
import { r as __toESM } from "./chunk-CYJPkc-J.js";
import { t as require_react } from "./react.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
import { t as ActivityCalendar } from "./index-CLOTiFb2-CpUeoiv9.js";
//#region node_modules/react-activity-calendar/build/index.js
var import_react = /* @__PURE__ */ __toESM(require_react());
var import_jsx_runtime = require_jsx_runtime();
//#endregion
//#region node_modules/react-github-calendar/build/index.js
var gitHubTheme = {
	light: [
		"#ebedf0",
		"#9be9a8",
		"#40c463",
		"#30a14e",
		"#216e39"
	],
	dark: [
		"#161b22",
		"#0e4429",
		"#006d32",
		"#26a641",
		"#39d353"
	]
};
var transformData = (data, transformFn) => {
	if (typeof transformFn !== "function") return data;
	const transformedData = transformFn(data);
	if (!Array.isArray(transformedData)) throw Error(`transformData() function must return a list of Activity objects.`);
	for (const d of transformedData) {
		if (!isRecord(d)) throw Error(`transformData() must return a list of valid Activity objects.`);
		if (typeof d.count !== "number" || d.count < 0) throw Error(`Required property "count: number" missing or invalid. Got: ${d.count}`);
		if (typeof d.date !== "string" || !/\d{4}-\d{2}-\d{2}/.test(d.date)) throw Error(`Required property "date: YYYY-MM-DD" missing or invalid. Got: ${d.date}`);
		if (typeof d.level !== "number" || d.level < 0 || d.level > 4) throw Error(`Required property "level: 0 | 1 | 2 | 3 | 4" missing or invalid: Got: ${d.level}.`);
	}
	return transformedData;
};
var isRecord = (o) => Object.prototype.toString.call(o) === "[object Object]";
async function fetchCalendarData(username, year) {
	const response = await fetch(`https://github-contributions-api.jogruber.de/v4/${username}?y=${String(year)}`);
	const data = await response.json();
	if (!response.ok) throw Error(`Fetching GitHub contribution data for "${username}" failed: ${data.error}`);
	return data;
}
var GitHubCalendar = /* @__PURE__ */ (0, import_react.forwardRef)(({ username, year = "last", labels, transformData: transformFn, throwOnError = false, errorMessage = `Error – Fetching GitHub contribution data for "${username}" failed.`, ...props }, ref) => {
	const [data, setData] = (0, import_react.useState)(null);
	const [loading, setLoading] = (0, import_react.useState)(false);
	const [error, setError] = (0, import_react.useState)(null);
	const fetchData = (0, import_react.useCallback)(() => {
		setLoading(true);
		setError(null);
		fetchCalendarData(username, year).then(setData).catch((err) => {
			if (err instanceof Error) setError(err);
		}).finally(() => {
			setLoading(false);
		});
	}, [username, year]);
	(0, import_react.useEffect)(fetchData, [fetchData]);
	if (error) if (throwOnError) throw error;
	else return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: errorMessage });
	if (loading || !data) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActivityCalendar, {
		...props,
		data: [],
		loading: true
	});
	const theme = props.theme ?? gitHubTheme;
	const defaultLabels = { totalCount: `{{count}} contributions in ${year === "last" ? "the last year" : "{{year}}"}` };
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActivityCalendar, {
		data: transformData(data.contributions, transformFn),
		labels: Object.assign({}, defaultLabels, labels),
		ref,
		...props,
		theme,
		loading: Boolean(props.loading) || loading,
		maxLevel: 4
	});
});
GitHubCalendar.displayName = "GitHubCalendar";
//#endregion
export { GitHubCalendar };

//# sourceMappingURL=react-github-calendar.js.map